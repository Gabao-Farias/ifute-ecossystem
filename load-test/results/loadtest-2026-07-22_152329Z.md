# Teste de carga — Caminho A (leitura) — 2026-07-22T15:23:29.291Z

Gerado automaticamente pelo k6 (`read-path.js`).

## Configuração da execução

| Parâmetro | Valor |
|---|---|
| Alvo (`BASE_URL`) | `https://api.ifute.com.br` |
| Pico alvo (`PEAK_RPS`) | 300 req/s |
| `PLACE_ID` informado | sim |
| `JWT` informado | não (cost-breakdown pulado) |
| Bypass rate limit | ativo (header enviado) |
| Início (UTC) | 2026-07-22T15:23:29.291Z |

## Resultado geral

| Métrica | Valor |
|---|---|
| Total de requests | 97918 |
| Throughput médio observado | 141.9 req/s |
| Taxa de erro (`http_req_failed`) | 92.95% |
| VUs máximos alocados | 64 |
| `http_req_duration` p50 (mediana) | 187ms |
| `http_req_duration` p90 | 197ms |
| `http_req_duration` p95 | 202ms |
| `http_req_duration` p99 | 216ms |
| `http_req_duration` max | 477ms |

## Latência por endpoint

| Endpoint | p50 | p90 | p95 | p99 | max |
|---|---|---|---|---|---|
| discover | 187ms | 197ms | 202ms | 216ms | 458ms |
| detalhes place | 187ms | 197ms | 202ms | 214ms | 477ms |
| city | 187ms | 197ms | 202ms | 217ms | 325ms |

## Thresholds (teto de capacidade)

Quando algum destes **ESTOUROU**, você passou do ponto de saturação sustentável.

| Métrica | Regra | Status |
|---|---|---|
| `lat_details` | `p(95)<800` | ✅ passou |
| `http_req_failed` | `rate<0.01` | ❌ ESTOUROU |
| `lat_discover` | `p(95)<800` | ✅ passou |
| `http_req_duration` | `p(95)<800` | ✅ passou |
| `http_req_duration` | `p(99)<2000` | ✅ passou |

## Correlação com o host (preencher a partir de `monitor-*.log`)

> Cole aqui os números do `monitor-host.sh` no instante em que a latência/erros
> dispararam. Objetivo: identificar o recurso que saturou primeiro.

| Recurso | Valor no pico | Saturou? |
|---|---|---|
| CPU load (1min) | | |
| Postgres conns / max_connections | | |
| Postgres queries ativas | | |
| Query ativa mais antiga (s) | | |
| Mem usada / livre | | |

## Leitura / conclusão

- **Teto observado:** ~____ req/s antes de erro > 1% ou p95 > 800ms.
- **Gargalo primário:** ____ (hipótese do repo: Postgres/pool de conexões antes da CPU).
- **Próximo passo:** ____ (ex: tuning de `max_connections`/pool do TypeORM, índices, cache).

---
_k6  — arquivo bruto: `loadtest-2026-07-22_152329Z.json`_
